#include "ProcessInfo.h"

// initilzer list
ProcessInfo::ProcessInfo(int processID, int arrivalTime, int durationTime, int memorySize):
_processID(processID), _arrivalTime(arrivalTime), _durationTime(durationTime), _memorySize(memorySize)
{

}